---
title: 工作区与文件夹
description: 在 HorseMD 工作区添加多个文件夹，并管理文件树。
---

# 工作区与文件夹

<span class="version-badge">适用于 HorseMD v0.13.29</span>

工作区是左侧文件树的容器，可以同时挂载多个磁盘文件夹。添加新文件夹不会替换已有文件夹，也不会把它们复制到 HorseMD 内部。

![包含多个目录的 HorseMD 工作区](/images/v0.12.10/workspace.png)
<span class="image-caption">根目录是工作区挂载点；其下文件和子目录可以展开、创建、重命名和删除。</span>

## 添加文件夹

1. 点击活动栏的文件夹图标。
2. 空工作区点击“添加文件夹”；已有内容时点击侧边栏顶部的文件夹加号。
3. 在系统对话框中选择目录。
4. 重复操作可以继续添加其他目录。

桌面端也可以直接把一个或多个文件夹从访达、文件资源管理器拖到 HorseMD 窗口。松开后，文件夹原位置和内容都不会改变，只会作为新的工作区根目录出现在左侧文件树；如果当前正在显示大纲，HorseMD 会自动切回文件树方便确认。

根目录本身不会被 HorseMD 重命名、拖走或删除。点击根目录右侧菜单可以从工作区移除它；“移除”只取消挂载，不会删除磁盘文件。

::: info 工作区不等于云同步
工作区只负责本地浏览和编辑。添加文件夹不会自动上传，也不会生成同步配置；需要同步时，再到“设置 → 云同步”明确添加该文件夹并连接 WebDAV 或 S3。反过来，从工作区移除目录也不会停止已经配置的云同步，请到云同步设置中执行“停止管理”。
:::

## 文件操作

在文件或文件夹上右键，可以新建、重命名、复制、打开所在位置或删除。删除使用系统废纸篓/回收站。拖动普通文件和子目录可以在同一工作区内移动，但不能把根挂载点当作普通文件夹移动。

拖动文件或文件夹时，侧栏底部会浮现“移到废纸篓”投放区；把它拖到该区域松开，项目即被移入系统废纸篓，之后可从访达/回收站恢复。投放时不会弹出确认框。

侧栏顶部的四个按钮分别用于：添加外部文件夹、新建文件、新建子文件夹，以及展开或折叠全部目录。添加文件夹使用带加号的文件夹图标，新建文件夹使用独立的目录创建图标。

## 自动恢复与监听

工作区路径会随会话保存，重新打开 HorseMD 后自动恢复。已挂载目录发生新增、删除或重命名时，文件树会自动刷新。系统根目录和受限路径不能被添加，以避免权限错误和大规模文件监听。
